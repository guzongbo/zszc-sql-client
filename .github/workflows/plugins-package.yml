name: plugins-package

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_INCREMENTAL: 0
  SCCACHE_GHA_ENABLED: "true"
  RUSTC_WRAPPER: sccache

concurrency:
  group: plugins-package-${{ github.ref }}
  cancel-in-progress: true

jobs:
  discover-plugins:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.collect.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Collect plugin matrix
        id: collect
        shell: bash
        run: |
          matrix="$(
            python3 - <<'PY'
            import json
            from pathlib import Path

            plugins = []
            for manifest in sorted(Path("plugins").glob("*/package-plugin.mjs")):
                plugin_dir = manifest.parent.as_posix()
                plugins.append({
                    "plugin_name": manifest.parent.name,
                    "plugin_dir": plugin_dir,
                    "frontend_dir": f"{plugin_dir}/frontend",
                    "backend_dir": f"{plugin_dir}/backend",
                })

            if not plugins:
                raise SystemExit("未找到可打包插件")

            print(json.dumps({"include": plugins}, ensure_ascii=False))
            PY
          )"
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  package-plugin:
    needs: discover-plugins
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.discover-plugins.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            ${{ matrix.plugin_dir }}/package-lock.json
            ${{ matrix.frontend_dir }}/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin,x86_64-pc-windows-msvc

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Cache Rust dependencies and build outputs
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: package-plugin-${{ runner.os }}-${{ matrix.plugin_name }}
          cache-workspace-crates: "true"
          workspaces: |
            ${{ matrix.backend_dir }} -> target

      - name: Install Windows cross-build dependencies
        shell: bash
        run: |
          if ! command -v brew >/dev/null 2>&1; then
            echo "brew 不可用，无法安装 Windows 交叉编译依赖" >&2
            exit 1
          fi
          brew list llvm >/dev/null 2>&1 || brew install llvm
          brew list lld >/dev/null 2>&1 || brew install lld
          cargo xwin --version >/dev/null 2>&1 || cargo install --locked cargo-xwin

      - name: Install plugin dependencies
        run: |
          npm ci --prefix ${{ matrix.plugin_dir }}
          npm ci --prefix ${{ matrix.frontend_dir }}

      - name: Build plugin package
        run: npm --prefix ${{ matrix.plugin_dir }} run package:all

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.plugin_name }}
          path: ${{ matrix.plugin_dir }}/dist/*.zszc-plugin
          if-no-files-found: error
          retention-days: 7
